import { CSSObject } from "styled-components";

export declare type Fonts = {
  body: {
    fontSource: CSSObject;
    fontFamily: string;
  };
  title: {
    fontSource: CSSObject;
    fontFamily: string;
  };
  subtitle: {
    fontSource: CSSObject;
    fontFamily: string;
  };
};
